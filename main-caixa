/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package caixa;

import javax.swing.JOptionPane;

/**
 *
 * @author Gabriel e Luan
 */
public class Caixa {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        float produto;
        float total = 0;
        int menu;
        do{
            produto = Float.parseFloat(JOptionPane.showInputDialog(null,"Entre com o valor do produto separado por ponto final.\n Digite '0' para sair."));
            total = total + produto;
        }while(produto!=0);
        JOptionPane.showMessageDialog(null,"SubTotal = R$"+total+".");
        menu = Integer.parseInt(JOptionPane.showInputDialog(null,"Escolha o método de pagamento:\n1 - Dinheiro\n2 - Cheque\n3 - Cartão de Crédito"));
        menu(menu);
        }
    }
    
class metodos{
    void menu(int menu){
        switch(menu){
            case 1:
                dinheiro(float total);
                break;
            case 2:
                cheque(float total);
                break;
            case 3:
                cartao(float total);
                break;
            default:
                JOptionPane.showMessageDialog(null,"Opção Inválida! Tente novamente.");
                menu(menu);
                break;
        }
    }
    float dinheiro(float total){
        float novototal = total * 0.9;
        
    }
    float cheque(float total){
        
    }
    float cartao(float total){
        
    }
}
